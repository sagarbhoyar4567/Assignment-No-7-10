Calculate the mean, median, and mode of 'Age'.

Find the standard deviation and variance of 'Salary'.

Compute the skewness and kurtosis of 'Purchase_Count'.

What is the correlation between 'Salary' and 'Satisfaction_Score'?

Get the summary statistics (count, mean, std, min, 25%, 50%, 75%, max) for every column.

Plot the histogram of 'Visit_Frequency' and comment on its skewness.

Identify if 'Satisfaction_Score' has any outliers using standard deviation method.

Calculate the coefficient of variation for 'Salary'.

Find the interquartile range (IQR) for 'Purchase_Count'.

Compute the Pearson correlation matrix for the entire dataset.

Calculate the range (max - min) for the 'Age' and 'Salary' columns.

Determine the percentile ranks for 'Purchase_Count' at 25%, 50%, and 75%.

Find the covariance between 'Salary' and 'Visit_Frequency'.

Check if 'Salary' follows a normal distribution using skewness and kurtosis values.

Calculate the z-scores for 'Satisfaction_Score' and identify any data points beyond ±3.


-----------------------------------

Create a pivot table to find the average Salary by each Category (group by Age groups if preferred).

Use a pivot table to sum the Purchase_Count for each Satisfaction_Score value.

Build a pivot table to show the count of records per Visit_Frequency value.

Generate a pivot table that shows the minimum, maximum, and average Salary per Age group (e.g., 18-25, 26-35, etc.).

Create a pivot table to display the total Purchase_Count for each distinct Salary range bucket (e.g., 25k-50k, 50k-75k, etc.).

---------------------------------

Load the Data in Power BI and Plot all the charts used in the Pivot Table

#### Create a Table Visual

Display all columns (Age, Salary, Purchase_Count, Satisfaction_Score, Visit_Frequency) in a table.

#### Build a Bar Chart

Plot the average Salary for each distinct Age.

Alternatively, group Age into bins (such as 18–25, 26–35, etc.) and show average Salary per age group.

Satisfaction Score Distribution

### Create a histogram or column chart showing the frequency of each Satisfaction_Score bucket (e.g., 1–2, 2–3, etc.).

Total Purchases by Age Group

Add a new “Age Group” column (e.g., 18–25, 26–35, etc.).

#### Create a bar/column chart of total Purchase_Count by Age Group.

Salary and Purchase Correlation

#### Create a scatter plot with Salary on the x-axis and Purchase_Count on the y-axis.

#### Add a trend line to see if there’s any positive or negative correlation.

KPI Visual

#### Add a KPI card to display the overall average Satisfaction_Score of all customers.

#### Slicer for Visit Frequency

Add a slicer that allows users to filter visuals by Visit_Frequency and see how metrics change.

#### Pie/Donut Chart

Visualize the distribution of customers by Salary ranges (e.g., 25k–50k, 50k–75k, etc.).


